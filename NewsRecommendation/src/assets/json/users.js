// Using declare variable with JSON to define User object
var User = {
  email: null,
  user_name: null,
  //password: null,
  firebase_user: null,  // Store JSON user obj returned from firebase auth
  unauthorized: true
}
